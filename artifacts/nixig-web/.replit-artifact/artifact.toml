kind = "web"
previewPath = "/"
title = "Nix Ig Downloader"
version = "1.0.0"
id = "artifacts/nixig-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26261

[services.development]
run = "pnpm --filter @workspace/nixig-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nixig-web", "run", "build" ]
publicDir = "artifacts/nixig-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26261"
BASE_PATH = "/"
